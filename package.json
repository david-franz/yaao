{
  "name": "yaao",
  "version": "0.0.1",
  "description": "yet another agent orchestrator",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "yaao": "./dist/bin/yaao.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "schema",
    "LICENSE"
  ],
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "tsup && node ./scripts/emit-config-schema.mjs && npm --workspace web run build && node ./scripts/copy-web-dist.mjs",
    "build:engine": "tsup && node ./scripts/emit-config-schema.mjs",
    "build:web": "npm --workspace web run build && node ./scripts/copy-web-dist.mjs",
    "dev": "tsup --watch",
    "dev:web": "npm --workspace web run dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "format": "prettier --write src tests docs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run lint && npm run test && npm run build"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.3",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "commander": "^12.1.0",
    "execa": "^9.3.0",
    "hono": "^4.12.21",
    "nanoid": "^5.0.7",
    "yaml": "^2.5.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@typescript-eslint/eslint-plugin": "^7.16.1",
    "@typescript-eslint/parser": "^7.16.1",
    "ajv": "^8.17.1",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "tsup": "^8.1.0",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0"
  }
}
